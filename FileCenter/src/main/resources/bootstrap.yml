spring:
  application:
    name: FileCenter
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: ConfigCenter
      profile: dev
      #是否启动快速失败功能，功能开启则优先判断config server是否正常
      fail-fast: true
server:
  port: 31910
#  port: 8089
eureka:
  client:
    serviceUrl:
      defaultZone: http://106.13.213.64:8086/eureka/
    registry-fetch-interval-seconds: 5
  instance:
    lease-expiration-duration-in-seconds: 15
    lease-renewal-interval-in-seconds: 5
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}
#    ip-address: 106.13.216.100
    ip-address: 2631a8815w.wicp.vip
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
